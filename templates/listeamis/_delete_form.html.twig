


<form method="post" action="{{ path('listeamis_delete', {'id': listeami.id}) }}" onsubmit="return confirm('Are you sure you want to delete this friend?');">


    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ listeami.id) }}">
    <button class="btn">Delete</button>
</form>
