{% extends 'base.html.twig' %}

{% block title %}Edit Messenger{% endblock %}

{% block body %}
    <h1>Edit Messenger</h1>

    {{ include('messenger/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('messenger_index') }}">back to list</a>

    {{ include('messenger/_delete_form.html.twig') }}
{% endblock %}
